import { NonBreakableHyphenPipe } from './non-breakable-hyphen.pipe';

describe('NonBreakableHyphenPipe', () => {
  it('create an instance', () => {
    const pipe = new NonBreakableHyphenPipe();
    expect(pipe).toBeTruthy();
  });
});

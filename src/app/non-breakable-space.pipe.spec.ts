import { NonBreakableSpacePipe } from './non-breakable-space.pipe';

describe('NonBreakableSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new NonBreakableSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
